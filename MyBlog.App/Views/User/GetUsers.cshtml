@model List<UserViewModel>
@{
	ViewData["Title"] = "List of users";
}
@{
    @if (Model.Count() == 0)
    {
	    <h5 class="card-title">Нет данных для отображения</h5>
    }
    else
    {
        <h5 class="card-title">Все пользователи</h5>
        @foreach (var user in @Model)
        {
            <div class="card w-75">
                <div class="card-body">
                    <h5 class="card-title">@user.FirstName</h5>
                    @foreach(var role in user.Roles)
                    {
                        <h5>Роль:</h5>
                            @role
                        <h5>Посты:</h5>
                    }                   
                </div>
            </div>
            if (User.IsInRole("Администратор"))
            {
            }
            <a class="btn btn-sm btn-primary" method="get" asp-controller="User" asp-action="GetUser" asp-route-id="@user.Id">Посмотреть профиль</a>
            <a class="btn btn-sm btn-primary" method="get" asp-controller="User" asp-action="EditUser" asp-route-id="@user.Id">Посмотреть профиль</a>
            <p></p>
        }
    }
}